# brujulavirtual-api-gateway

## Run the following command to start the API Gateway:

### Build the project:

```bash
cargo build
```

### Run the project:
```bash
cargo run
```

### Run Clean
```bash
cargo clean
```